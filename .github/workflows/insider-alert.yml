name: Insider Alerts (WhatsApp)

on:
  workflow_dispatch: {}
  schedule:
    # Draait elke 30 minuten
    - cron: "*/30 * * * *"

permissions:
  contents: read

jobs:
  alert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # fallback + Twilio/pyyaml zeker installeren
          pip install pyyaml twilio || true

      - name: Prepare config.local.yaml
        env:
          SEC_USER_AGENT: ${{ secrets.SEC_USER_AGENT }}
        run: |
          set -e
          # kopieer basis-config indien nog niet aanwezig
          if [ ! -f config.local.yaml ] && [ -f config.yaml ]; then
            cp config.yaml config.local.yaml
          fi
          python - <<'PY'
          import os, yaml
          p='config.local.yaml'
          cfg = {}
          if os.path.exists(p):
              cfg = yaml.safe_load(open(p)) or {}
          ua = os.getenv('SEC_USER_AGENT')
          if ua:
              cfg.setdefault('user_agent', {})['ua'] = ua
          with open(p, 'w') as f:
              yaml.safe_dump(cfg, f, sort_keys=False, allow_unicode=True)
          print("✅ config.local.yaml updated")
          PY

      - name: Debug repo layout
        run: |
          pwd
          ls -lah
          ls -lah scripts || true

      # BELANGRIJK: paths zijn relatief vanaf repo-root (GEEN dubbele insider-monitor/…)
      - name: Run insider pipeline
        run: |
          set -e
          python scripts/fetch_insiders.py --days 1 --config config.local.yaml
          python scripts/score_signals.py  --config config.local.yaml
          python scripts/fetch_news.py     --config config.local.yaml
          python scripts/report_builder.py --config config.local.yaml

      - name: Detect new signals & build message
        id: detect
        run: |
          python - <<'PY'
          import glob, os, csv
          files = sorted(
              glob.glob('data/reports/insider_signals_*.csv'),
              key=os.path.getmtime, reverse=True
          )
          alert = False
          message = "Geen nieuwe insider-signalen gevonden."
          if files:
              f = files[0]
              try:
                  with open(f, newline='') as fh:
                      rows = list(csv.reader(fh))
                  n = max(len(rows) - 1, 0)  # minus header
                  if n > 0:
                      alert = True
                      message = f"✅ {n} nieuw(e) insider-signaal/signalen. Zie rapport: {f}"
              except Exception as e:
                  message = f"⚠️ Kon rapport niet lezen ({e})."
          with open(os.environ['GITHUB_OUTPUT'],'a') as g:
              g.write(f"alert={'true' if alert else 'false'}\n")
              g.write(f"message={message}\n")
          PY

      - name: Send WhatsApp via Twilio (only if alert)
        if: steps.detect.outputs.alert == 'true'
        env:
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN:  ${{ secrets.TWILIO_AUTH_TOKEN }}
          ALERT_TO_INSIDER:   ${{ secrets.ALERT_TO_INSIDER }}  # bv. whatsapp:+31652615803
        run: |
          python - <<'PY'
          import os
          from twilio.rest import Client
          sid = os.getenv("TWILIO_ACCOUNT_SID")
          tok = os.getenv("TWILIO_AUTH_TOKEN")
          to  = os.getenv("ALERT_TO_INSIDER")
          msg = """${{ steps.detect.outputs.message }}"""
          if not all([sid, tok, to]):
              raise SystemExit("❌ Missing Twilio env vars")
          client = Client(sid, tok)
          m = client.messages.create(
              from_="whatsapp:+14155238886",  # Twilio WhatsApp SANDBOX
              to=to,
              body=f"📣 Insider Monitor alert:\n{msg}"
          )
          print("✅ WhatsApp sent, SID:", m.sid)
          PY

      - name: Upload reports (optional)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            data/reports/**
            data/reports/history/**
            data/reports/*.csv
            data/reports/*.md
